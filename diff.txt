diff --git a/gvr-particles/app/build.gradle b/gvr-particles/app/build.gradle
index 7b358bf8..16b1541b 100644
--- a/gvr-particles/app/build.gradle
+++ b/gvr-particles/app/build.gradle
@@ -25,7 +25,11 @@ android {
 
 dependencies {
     if(rootProject.useLocalDependencies) {
-        compile(name: 'gvrf-particlesystem-debug', ext: 'aar')
+        if (findProject(':gvrf-particlesystem')) {
+            compile project(':gvrf-particlesystem')
+        } else {
+            compile(name: 'gvrf-particlesystem-debug', ext: 'aar')
+        }
     } else {
         compile "org.gearvrf:gvrf-particlesystem:$gearvrfVersion"
     }
